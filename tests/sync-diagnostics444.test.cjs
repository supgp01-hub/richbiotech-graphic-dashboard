const fs=require('fs'),vm=require('vm'),assert=require('node:assert/strict');let sent;const values={};const w={_rbUser:{uid:'test',name:'TER'},rbFirebaseAuth:{fetch:async(url,o)=>{sent={url,body:JSON.parse(o.body)};return{ok:true};}}};const c={window:w,Response,localStorage:{getItem:k=>values[k],setItem:(k,v)=>values[k]=v},document:{addEventListener(){}},console};vm.createContext(c);vm.runInContext(fs.readFileSync('snippets/safe-order-write-v1.js','utf8'),c);vm.runInContext(fs.readFileSync('snippets/order-conflict-review-v1.js','utf8'),c);w.rbOrderConflictReview.report([{path:'/orders/key',conflict:true,data:{id:'TEST',brief:'PRIVATE-BRIEF',assignee:'JAM',status:'pending'}}],{key:{id:'TEST',brief:'OTHER-PRIVATE',assignee:'DOM',status:'review'}});assert.equal(sent.body.count,1);assert(!JSON.stringify(sent).includes('PRIVATE'));assert(sent.url.includes('/workflow_snapshots/order_sync_diagnostics_v1/test/'));assert.deepEqual(Array.from(sent.body.conflicts[0].fields),['brief','assignee','status']);console.log('PASS: diagnostics identify conflicting fields without uploading work text');
