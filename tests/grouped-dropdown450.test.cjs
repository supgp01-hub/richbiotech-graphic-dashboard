const fs=require('fs'),{JSDOM}=require('jsdom'),assert=require('node:assert/strict');const script=fs.readFileSync('index.html','utf8').match(/<script id="rb-global-dropdown-v5-script">([\s\S]*?)<\/script>/)[1];const dom=new JSDOM('<select><option value="">All</option><optgroup label="Ready"><option value="vacant">Vacant</option><option value="locked" disabled>Locked</option></optgroup></select>',{runScripts:'outside-only',pretendToBeVisual:true}),w=dom.window;w.HTMLElement.prototype.scrollIntoView=function(){};w.eval(script);w.document.dispatchEvent(new w.Event('DOMContentLoaded'));const s=w.document.querySelector('select');let changes=0;s.addEventListener('change',()=>changes++);s.dispatchEvent(new w.MouseEvent('pointerdown',{bubbles:true,cancelable:true}));const buttons=[...w.document.querySelectorAll('.rb-dd-option')];assert(buttons[2].disabled);buttons[1].click();assert.equal(s.value,'vacant');assert.equal(changes,1);assert.equal(w.document.querySelector('#rb-dd-popover').style.display,'none');w.close();console.log('PASS grouped dropdown commits selection and change event; disabled option protected');
