const {JSDOM}=require('jsdom'),assert=require('node:assert/strict'),fs=require('fs');
const d=new JSDOM('<main></main>',{runScripts:'outside-only'}),w=d.window;w._rbUser={role:'sup'};w.confirm=()=>true;w.eval(fs.readFileSync('snippets/planner-calendar-v1.js','utf8'));const api=w.rbPlannerCalendar,day=new Date(),iso=day.getFullYear()+'-'+String(day.getMonth()+1).padStart(2,'0')+'-'+String(day.getDate()).padStart(2,'0');let edits=0,deletes=0;const ctx={orders:[{id:'GR1',sourceDraftId:'d1',name:'Real',assignee:'TER',deadline:iso,type:'Clip',product:'A'}],drafts:[{id:'d1',status:'scheduled'},{id:'d2',name:'Draft',assignee:'NUNE',deadline:iso,type:'Graphic',product:'B'}],members:['TER','NUNE'],leave:(date,n)=>n==='NUNE',add(){},edit(){edits++},remove(){deletes++;return Promise.resolve()},refresh(){}};assert.equal(api.merge(ctx.orders,ctx.drafts).length,2);const h=w.document.querySelector('main');api.render(h,ctx);assert.equal(h.querySelectorAll('[data-pc-task]').length,2);assert.ok(h.querySelector('.pc-leave'));const f=h.querySelector('[data-pc-filter="employee"]');f.value='TER';f.dispatchEvent(new w.Event('change',{bubbles:true}));assert.equal(h.querySelectorAll('[data-pc-task]').length,1);h.querySelector('[data-pc-task]').click();h.querySelector('[data-pc-edit]').click();assert.equal(edits,1);h.querySelector('[data-pc-delete]').click();assert.equal(deletes,1);console.log('calendar merge, employee filter, leave, edit and confirmed delete passed');
