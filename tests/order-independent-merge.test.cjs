const fs=require('fs'),vm=require('vm'),assert=require('assert');const ctx={window:{},Response};vm.runInNewContext(fs.readFileSync('snippets/safe-order-write-v1.js','utf8'),ctx);
(async()=>{let saved;const remote={id:'T',assignee:'JAM',status:'inprogress',brief:'original',updatedAt:20};const request=async(u,o)=>{if(o.method){saved=JSON.parse(o.body);return new Response('{}');}return new Response(JSON.stringify(remote),{headers:{ETag:'1'}});};await ctx.window.rbSafeOrderWrite.write('test',{method:'PATCH',body:JSON.stringify({brief:'new',updatedAt:30}),rbBaseUpdatedAt:10,rbBaseValues:{brief:'original'}},request);assert.equal(saved.assignee,'JAM');assert.equal(saved.status,'inprogress');assert.equal(saved.brief,'new');await assert.rejects(()=>ctx.window.rbSafeOrderWrite.write('test',{method:'PATCH',body:JSON.stringify({assignee:'DOM',updatedAt:30}),rbBaseUpdatedAt:10,rbBaseValues:{assignee:'MOS'}},request));console.log('PASS independent field merge and conflicting assignee blocked');})().catch(e=>{console.error(e);process.exitCode=1;});
